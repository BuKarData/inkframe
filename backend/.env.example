# InkFrame API Configuration
# Copy this file to .env and fill in your values
# For Railway deployment, set these as environment variables in Railway dashboard

# Server Configuration
# Railway automatically sets PORT, so don't set it manually in Railway
PORT=3000
NODE_ENV=production
API_URL=https://eink-luvia.com

# Authentication
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# Database (using JSON file storage for simplicity)
# For production, use PostgreSQL or MongoDB
DATA_DIR=./data

# OpenWeatherMap API
# Get your free API key at: https://openweathermap.org/api
OPENWEATHER_API_KEY=your-openweathermap-api-key

# Google Calendar API (OAuth2)
# Set up at: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/google/callback

# Todoist API
# Get your API token at: https://todoist.com/app/settings/integrations
TODOIST_CLIENT_ID=your-todoist-client-id
TODOIST_CLIENT_SECRET=your-todoist-client-secret

# Image Storage
# For development, uses local file storage
# For production, configure AWS S3 or similar
STORAGE_TYPE=local
UPLOAD_DIR=./uploads
MAX_IMAGE_SIZE=5242880

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Logging
LOG_LEVEL=debug
